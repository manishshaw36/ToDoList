export interface Item {
    taskname?: string;
    taskcatogory?: string;
    taskpriority?: string;
    description?: string;
    date?: string;
    time?: string;
    report?: string;
    id?: string;
  }

export interface Filter {
  filterValue: string;
}
